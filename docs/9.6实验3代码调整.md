# 9.6 实验代码调整说明

本次调整依据两类问题：一是 8.30 结果中 Ours 在 LongMemEval N=10 上低于 Full Context 和 mem0；二是学长指出实验三 `320 tokens` 不符合正常长任务预算、技能实验 Manual 与 Ours 全部指标相同且不自然。调整目标是提高实验的公平性和可解释性，不通过删样本、改旧结果或人为抬高分数来获得“更好看”的数字。

旧结果保留在原目录，仅作为诊断记录。以下代码变更后产生的结果必须写入新输出目录，不能和 `8.30实验3结果.md` 的旧表混合。

## 一、长期记忆实验：从 Top-5 整段水合改为候选召回加证据片段选择

### 旧问题

旧 Ours 只取 Top-5 会话，并把每个命中归档的整段原文水合后交给 QA。N=10 结果显示：

- Ours Full 的 QA Acc 为 0.4，低于 Full Context 的 1.0 和 mem0 的 0.7；
- Ours 的 Evidence Recall 为 0.525，多证据、时间和知识更新问题经常漏掉必要会话；
- 五段原始会话仍导致约 17k context tokens，虽然低于无限 Full Context，但明显高于 mem0；
- Ours Full 的更新 Judge 443 次调用只产生 2 次 UPDATE，成本高但收益不稳定。

### 新实现

`engine/experiments/memory.py` 新增两级流程：

1. `candidate_k=20`：Hybrid 检索先返回最多 20 个候选会话，用于 session-level Hit@K、MRR、Evidence Recall；
2. `context_token_budget=12000`：对候选原始会话按 1800 字符切片，依据问题词重合与会话排名选择证据片段，再限制送给 QA 的上下文；
3. QA 指标与检索指标分离：QA 使用证据片段，Hit@K/MRR 仍按完整候选会话及 gold evidence session 计算；
4. 新增 `full_context_budgeted` 方法：按时间最近原则截断 Full Context 到与 Ours 相同的 token budget。它是公平预算基线；无限 Full Context 仍保留为性能上界。

每条结果 metadata 中新增：

- `candidate_sessions`：会话级候选数量；
- `evidence_selection.selected_chunks`：交给 QA 的片段数；
- `evidence_selection.selected_tokens`：片段 token；
- `context_policy`：Full Context 是否采用最近历史截断。

### 正式运行协议

旧 `longmemeval_final_10.jsonl` 已被用于诊断和调参，应视为开发集。重新按题型分层抽取未见测试集，建议至少 20-30 条，并冻结 JSONL、seed、SHA-256、模型名和 prompt 后再运行：

```powershell
python examples/experiments/prepare_datasets.py memory `
  --input data/raw/longmemeval/longmemeval_s.json `
  --source longmemeval `
  --output data/processed/longmemeval_test_30.jsonl `
  --count 30 --seed 20260906 --stratified
```

假设新的冻结文件为 `data/processed/longmemeval_test_30.jsonl`：

```powershell
python examples/experiments/run_memory_experiment.py `
  --dataset data/processed/longmemeval_test_30.jsonl `
  --source longmemeval --backend no_memory `
  --output-dir runs/experiments/memory_v2 --run-name no_memory

python examples/experiments/run_memory_experiment.py `
  --dataset data/processed/longmemeval_test_30.jsonl `
  --source longmemeval --backend full_context `
  --output-dir runs/experiments/memory_v2 --run-name full_context_unbounded

python examples/experiments/run_memory_experiment.py `
  --dataset data/processed/longmemeval_test_30.jsonl `
  --source longmemeval --backend full_context_budgeted `
  --context-token-budget 12000 `
  --output-dir runs/experiments/memory_v2 --run-name full_context_recent_12k

python examples/experiments/run_memory_experiment.py `
  --dataset data/processed/longmemeval_test_30.jsonl `
  --source longmemeval --backend engine --no-llm-judge `
  --candidate-k 20 --context-token-budget 12000 `
  --output-dir runs/experiments/memory_v2 --run-name ours_no_judge

python examples/experiments/run_memory_experiment.py `
  --dataset data/processed/longmemeval_test_30.jsonl `
  --source longmemeval --backend engine `
  --candidate-k 20 --context-token-budget 12000 `
  --output-dir runs/experiments/memory_v2 --run-name ours_full

python examples/experiments/run_memory_experiment.py `
  --dataset data/processed/longmemeval_test_30.jsonl `
  --source longmemeval --backend mem0 `
  --output-dir runs/experiments/memory_v2 --run-name mem0
```

主表使用 QA Acc、Answer F1、Evidence Recall、MRR、Avg Context Tokens、Avg Time。Full Context 两组没有可排序的 Top-K，Hit@K/MRR 填 `N/A`。不要将候选会话 Hit@5 与最终 QA 成功等同。

若 Ours Full 仍然没有相对 no Judge 的稳定收益，主表应保留 Ours Full 的成本和动作数，但不能声称更新 Judge 有增益。应另建“旧事实-新事实-干扰事实”的知识更新诊断集，专门报告 UPDATE precision/recall、Stale Answer Rate 和 Judge latency。

## 二、长期记忆与上下文治理：16k 真实预算和完整历史

### 旧问题

旧实验把最大上下文设为 320 token，Full Context 通过重复 `memory_fact + distractor` 24 次保证超限；没有 BudgetController 的方法又会被直接记为预算违规。每条任务只写入一条已经包含答案的历史事实，无法代表真实长任务中的检索。

### 新实现

`engine/experiments/long_task.py`、`engine/experiments/datasets.py` 调整为：

- 默认 `max_context_tokens=16384`，保留输出预算 `2048`，有效输入预算为 14,336；
- LoCoMo 派生任务增加 `history` 字段，保存原问题的全部历史会话；
- Memory Only/Ours Full 写入全部历史后再检索，Full Context 直接暴露真实历史；不再重复一条答案事实制造长度；
- `Budget Violation` 仅在实际可见上下文超过预算、且方法没有预算控制时为真；
- 新增 `history_tokens`、`budget_limit`，用于判断每条任务是否真的形成预算压力。

该实验仍是确定性后端机制联合评测，不是 LLM Agent 的真实端到端任务成功率。新的结果不应再出现“所有无 Budget 方法必然预算违规”的固定模式；如果真实历史没超过 16k，预算违规为 0 是正常结果。

### 重新构建和运行

必须重新生成数据，因为旧 JSONL 没有 `history` 字段：

```powershell
python examples/experiments/prepare_datasets.py state `
  --input data/raw/locomo/locomo10.json `
  --output data/processed/locomo_state_100_history_v4.jsonl `
  --count 100 --seed 42

python examples/experiments/run_long_task_experiment.py `
  --dataset data/processed/locomo_state_100_history_v4.jsonl `
  --method all `
  --max-context-tokens 16384 `
  --reserved-output-tokens 2048 `
  --output-dir runs/experiments/long_task_history_v4
```

主表保留 Goal Retention、Constraint Compliance、Memory Use Accuracy、Drift Rate、Budget Violation、Recovery Success、Avg Tokens，并额外报告 Avg History Tokens。`Final Task Success` 只能标注为 deterministic mechanism conjunction，不得写成“真实长任务成功率”。

## 三、技能闭环：训练/测试隔离和关键步骤覆盖

### 旧问题

旧实现对每个样本先用自己的 `trajectory` 创建技能，再用同一条样本的 `expected_steps` 检查覆盖率；Manual 与 Ours 使用相同步骤，因此 30 条上几乎所有指标都相同。这是自检索泄漏，不能说明可复用性或泛化能力。

### 新实现

`engine/experiments/skills.py` 与 `datasets.py` 改为：

1. 每个任务类型 10 条中，前 3 条标为 `train`，后 7 条标为 `test`；
2. Ours 只从 3 条训练轨迹归纳并发布一份技能；Manual 只提供每类一份固定人工基础技能；
3. 只对 21 条 held-out test task 检索并评测，测试样本不会创建自己的技能；
4. 新增 `critical_step_coverage`，用于检查训练轨迹中沉淀的环境特定步骤，例如邮件的“检查发送权限”、日历的“确认时区”、旅行规划的“核验预算”；
5. 新增 `automatic_skill_generation_success`、`training_source_examples`、`manual_skill_provided`，区分自动沉淀与人工提供；
6. `Task Success` 仍是 procedural-plan coverage proxy，不是 LLM 真实任务成功率。

### 重新构建和运行

```powershell
python examples/experiments/prepare_datasets.py skills `
  --output data/processed/skill_reuse_30_split_v2.jsonl `
  --count 30 --seed 42

python examples/experiments/run_skill_experiment.py `
  --dataset data/processed/skill_reuse_30_split_v2.jsonl `
  --limit 30 --method all --train-per-type 3 --fresh `
  --output-dir runs/experiments/skills_split_v2
```

新结果每种方法的测试样本数应为 21，而不是 30；metadata 应显示 `train_examples=9`、`test_examples=21`。Manual 与 Ours 即使 Task Success 都相同，也应在 Step Coverage、Critical Step Coverage、Automatic Generation 和 Training Source Examples 上呈现不同的机制含义。论文中可表述“自动技能在未见任务上达到或超过固定基础技能的步骤覆盖”，不可表述为“真实任务成功率 100%”。

## 四、验收情况

本次修改新增/更新离线测试，执行：

```powershell
py -3.12 -m pytest tests\test_experiments.py --basetemp tmp\pytest_experiments -q
```

实际验证结果：`22 passed`。

已完成的样例冒烟：

- 新技能数据 30 条生成成功，三类均为 3 条训练 + 7 条测试；测试输出为 21 条；
- Manual Skill 的 Step Coverage 为 0.75、Critical Step Coverage 为 0；Ours Skill Loop 均为 1.0，且自动生成成功标记为 1；
- LoCoMo 2 条样例在 16k 预算下，Full Context 的真实历史约 18k token，因实际超限才记录预算违规；Memory/Ours 的可见检索上下文约数百 token，不再被固定判为预算违规；
- 新长期记忆证据选择器已通过离线预算测试，正式 LongMemEval 结果尚未重新运行。

注意：上述样例不是新的正式论文结果。正式运行必须使用冻结的未见测试集、独立输出目录，并同时保存 `summary.json`、`rows.jsonl`、数据 SHA-256、模型版本、prompt 与参数。

## 五、后续重构与本地 BGE-M3 运行记录（2026-09-07）

### 本轮修复

1. `BGEM3EmbeddingModel` 兼容本机 FlagEmbedding 返回的 `dense_vecs` 字段；此前只读取 `dense_embeds`，会导致本地 BGE-M3 实际不可用。
2. LoCoMo 原始历史是逐句列表。检索前改为按时间顺序合并为约 600 字符的连续对话片段；避免把每句当成独立文档。
3. 检索改为两阶段：词法高召回候选（20 个）后由 BGE-M3 稠密重排，最终保留 Top-5 证据。数据构建脚本新增 `--dense-candidate-k`，仅保留候选阶段确实能召回答案证据的诊断题。
4. Full-Context 不再无视预算直接提交全部历史，而是在同一 16,384 上下文窗口、预留 2,048 输出 token 的约束下保留最新历史；本次实际可见历史预算为 12,000 token。
5. QA 生成器与判定器统一为 `temperature=0` 和关闭 thinking 的模式；此前仅 judge 关闭 thinking，长上下文生成耗时不稳定。
6. BGE 向量缓存与方法运行目录分离。首次建库成本不能计入某一个方法的在线时延；正式比较应预建索引后测量在线时间。

### 冻结诊断集与运行命令

正式本地运行数据集为 `data/processed/locomo_directfact_hybrid20_24_v12.jsonl`：从 LoCoMo 抽取 24 条直接事实题，要求答案证据能进入 20 个词法候选窗口。该条件是诊断集的适用范围，不能宣称它代表完整 LoCoMo。

```powershell
$env:BGE_M3_MODEL_PATH = 'D:\PythonProject\hf_cache\models\BAAI--bge-m3'
$env:HF_HOME = 'D:\PythonProject\hf_cache'
$env:TMP = 'D:\PythonProject\long_task_1\long_task_1\tmp\bge_tmp'
$env:TEMP = $env:TMP

D:\SoftWare\Anaconda\envs\Lang_Task\python.exe examples\experiments\run_long_task_experiment.py `
  --dataset data\processed\locomo_directfact_hybrid20_24_v12.jsonl `
  --method all --qa-solver llm --embedding-backend bge_m3 `
  --candidate-k 20 --top-k 5 --evidence-token-budget 12000 `
  --max-context-tokens 16384 --reserved-output-tokens 2048 `
  --output-dir runs\experiments\long_task_directfact_hybrid20_bge_v12
```

### 本次结果与解释

输出目录：`runs/experiments/long_task_directfact_hybrid20_bge_v12`。

| 方法 | QA Acc | Answer F1 | 证据命中 | 平均上下文 token | 漂移率 | 恢复成功 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Plain | 0.000 | 0.000 | 0.000 | 0 | 1.000 | 0.000 |
| Full-Context | 0.167 | 0.122 | 0.500 | 12,310 | 1.000 | 0.000 |
| Memory Only | 0.208 | 0.190 | 1.000 | 2,746 | 1.000 | 0.000 |
| Ours Full | 0.250 | 0.231 | 1.000 | 2,746 | 0.000 | 1.000 |

Memory/Ours 相比 Full-Context 将平均可见上下文减少约 77.7%，并提高 QA 指标；Ours 与 Memory Only 使用同一检索结果，联合实验的独立贡献应解释为漂移控制和检查点恢复，不应把两者很小的 QA 差异归因于治理模块。首次 BGE 建库约 24 条缓存文件、约 10.9 MB；预建索引后的在线平均耗时为 Plain 1331 ms、Full-Context 1226 ms、Memory Only 1495 ms、Ours Full 2283 ms。

这组结果仍不适合作为论文主表：虽然字符串证据命中为 1.0，但 QA Acc 仅 0.25，说明当前直接事实筛选只验证了答案字符串出现，未保证所有多实体/多跳问题的关系证据都被同一组片段覆盖。下一版主实验应使用 LongMemEval 的标准 QA 切分，或人工审核 30 条单跳、关系完整的 LoCoMo 问题；不得仅凭本诊断集报告“长期记忆 QA 很强”。

## 六、端到端多轮重构（v10，替代第五节旧 runner）

第五节记录的 v7 runner 仅用于保留问题追踪，现已由 v10 端到端实现替代。新流程固定为六轮：目标建立、任务推进、新增约束、冲突干扰、动态预算压力、最终 QA；第三轮后销毁运行时对象，并按方法允许的持久化状态恢复。

- 每一轮 Agent 输入都由当前目标、当前约束、当前用户事件、严格 Top-5 证据、近期轨迹组成；Ours 额外注入实时 ContextLedger 和漂移治理决定。
- 所有方法面对同一故障类型和动态预算。恢复成功要求在三轮内同时恢复故障前进度、原目标和有效约束，而不是只检查 checkpoint 文件存在。
- `Drift Rate` 为裁判判定的漂移轮数除以总轮数；`Goal Retention` 和 `Constraint Compliance` 均从逐轮行为计算，不再写死。
- `Budget Violation Rate` 根据每轮治理后的真实上下文判断；另报 `Budget Action Rate` 和 `Budget Control Success`。Ours 的压缩动作调用项目 `ContextBudgetController`，决定写回 Ledger。
- `Context Tokens` 改为每轮完整 Agent 上下文的均值；另报 `Retrieved Memory Tokens`、`Ledger Tokens`、`QA Context Tokens` 和 `Total Context Tokens`。因此 Memory Only 与 Ours 不再出现总上下文完全相同的假象。
- LoCoMo 的 `D1:3` 类 gold evidence ID 已解析回原始说话人与文本。新数据集 `data/processed/locomo_multiturn_gold_24_v10.jsonl` 保留每题最多两条 gold evidence。
- BGE-M3 先对 20 个词法候选做稠密重排，再严格返回 5 个片段。新增 `Gold Evidence Recall@5`；证据充分性由 LLM 同时查看问题、参考答案、gold evidence 和 Top-5 证据判定。
- 每条任务保存独立 trajectory JSON，包括输入上下文、用户事件、action、observation、预算决定、Top-5 证据和逐轮裁判结果；不记录隐藏思维链。

正式命令改为：

```powershell
D:\SoftWare\Anaconda\envs\Lang_Task\python.exe examples\experiments\run_long_task_experiment.py `
  --dataset data\processed\locomo_multiturn_gold_24_v10.jsonl `
  --method all --qa-solver llm --trajectory-judge llm `
  --embedding-backend bge_m3 --candidate-k 20 --top-k 5 `
  --max-context-tokens 16384 --reserved-output-tokens 2048 `
  --output-dir runs\experiments\long_task_multiturn_bge_v10
```

先运行 `--limit 2` 检查 API、结构化 JSON 和 BGE 缓存，再去掉 `--limit`。离线回归测试为 `26 passed`；四条 Hashing 冒烟仅验证执行链和指标公式，不是论文结果。正式结果必须使用上面的 BGE-M3 + LLM 配置重新运行。
