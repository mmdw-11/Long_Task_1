# 本项目依赖：接入 AgentScope agent、OpenAI 模型，以及前端可视化 REST 服务。

# AgentScope（真实 LLM agent 节点）。需 Python>=3.10 才能装到 2.x。
agentscope>=2.0.0

# OpenAI 模型 + 从 .env 读取配置
openai>=1.30.0
python-dotenv>=1.0.0

# 记忆模块 embedding（BGE-M3 稠密向量检索）
# 安装后可使用 BGEM3EmbeddingModel 替代默认的 HashingEmbeddingModel
FlagEmbedding>=1.2.0

# 本地 BGE-M3 端—边—云路由：SentenceTransformer 编码 + MLP 分类器。
# 调度器默认加载该路由；依赖缺失时才会安全回退到启发式 Gate。
sentence-transformers>=3.0.0
scikit-learn>=1.4.0

# travel_planner 示例的 Web 工具
# ddgs：DuckDuckGo 搜索库，用于 web_search 工具
# playwright：真实浏览器，用于 browse_webpage 工具
# 安装 playwright 后需执行：python -m playwright install chromium
ddgs>=9.0.0
playwright>=1.40.0

# REST 服务（前端可视化编排接口）
fastapi>=0.110.0
python-multipart>=0.0.9
cryptography>=42.0.0
httpx>=0.27.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# Knowledge-base document parsing.  These are deliberately lightweight and
# enable the formats exposed by the upload UI without calling an external API.
pypdf>=5.0.0
PyMuPDF>=1.28.0
python-docx>=1.1.0
openpyxl>=3.1.0
beautifulsoup4>=4.12.0

# 测试
pytest>=7.0.0
pytest-asyncio>=0.21.0
